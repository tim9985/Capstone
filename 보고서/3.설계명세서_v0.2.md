# 창의융합종합설계 설계 명세서 (v0.2)

| | |
|---|---|
| 작품 명칭 | 자율 정찰 드론 관제 시스템 |
| 개발기간 | 2026.09.01 ～ 2026.12.09 |
| 책임자 | 이시원 |
| 참여학생 | 서성훈 · 정서인 |
| 근거 문서 | 요구명세서 v1.7 (UC 84 · NFR 20 · AT 22) · 캡스톤 디자인 계획서 §3.2 최종 예산안 |

> 작성 상태: 비전 파트(정서인) 실측 근거까지 작성 · 관제·비행 파트는 골격 — 담당자 보완
> 표기: `[확정]` 실측·합의 완료 · `[잠정]` 근거 있으나 변경 가능 · `[TBD]` 결정 대기
> 학번·연락처는 hwp 제출본에만 기입 (공개 저장소)

---

# 1. 개요

## 1.1 시스템의 목표

- 고령 인구가 많은 농촌·산지에서 **드론이 자율 정찰하며 사람(쓰러진 사람 포함)을 찾는다**
- 탐지한 사람의 **화면 좌표를 실좌표로 바꿔 관제 지도에 표시**한다
- 운영자는 스냅샷을 보고 대응 여부를 **직접 판단**한다 — 시스템은 후보만 제시

| 목표 | 설계상 의미 |
|---|---|
| 자율 정찰 | 관측 완료도를 계산해 **미관측 영역을 스스로 고른다** (UC-08xx) |
| 사람 탐지 | 누운 사람·부분 가림 포함 **1클래스(person)** · 자세 자동 분류 없음 |
| 좌표 신고 | 박스 아래 변(발끝) 광선–지면 교차로 실좌표 산정 (NFR-V04) |
| 운영자 판단 | 후보 제시 → 운영자 판정 (UC-0705) |

## 1.2 소프트웨어의 주요 기능

| 구분 | 기능 | 대응 UC |
|---|---|---|
| 관제 | 로그인·권한·제어권, 지도·레이어, 임무구역 지정, 알림 | UC-01xx · 03xx · 04xx · 11xx |
| 비행 | 기체 상태·통신, 탐색 후보 생성·관측지점 선택, 명령 검증·전달, 안전·장애 | UC-02xx · 05xx · 09xx · 10xx |
| **영상** | **영상 수신·시각 동기, 사람 탐지, 스냅샷, 상의 색상, 좌표 산정** | **UC-06xx · 07xx** |
| 공통 | 관측 완료도, 임무 기록·조회·내보내기 | UC-08xx · 12xx |

## 1.3 설계상 제약사항

### 하드웨어 (최종 예산안 · 15품목 1,867,810원) `[확정]`

| 구분 | 사양 | 설계 영향 |
|---|---|---|
| 기체 | F450 · KoA H743 FC · M10 GPS | MAVLink 로 자세·위치 수신 |
| 카메라 | RunCam WiFiLink 2-G · IMX415 · **1920×1080** · H.264 | 입력 해상도 고정 |
| 렌즈 | **주 렌즈 LN012 대각 88°** (수평 80.2° · 수직 50.7° · f 1,141 px) · 보조 62° · 78° | 사람 크기·좌표 식의 f 값 · 보조 렌즈는 기체 호환 확인 후 |
| 짐벌 | CaddxFPV GM3 V2 3축 | 마운트각을 **비행 중 지정** · 흔들림 보정 |
| 게이트웨이 | Raspberry Pi 4B 2GB | **추론하지 않는다** — 중계·순환 기록만 |
| 서버 | RTX 4080 SUPER (운용) · RTX 3090 (개발) | TensorRT 엔진은 **운용 GPU 에서 재빌드** |
| 전방 센서 | Benewake TF02-Pro | 비행 담당 (충돌 회피) |

### 운용 조건

| 항목 | 값 | 상태 | 근거 |
|---|---|---|---|
| 마운트각 | **기본 하향 60°** · 대응 45~75° | `[잠정]` | 30 m 에서 서 있는 사람 90° 31 px → **60° 51 px** (1.65배) |
| 운용 고도 | 16~30 m | `[잠정]` | |
| 지상 촬영 범위 (60° · 30 m) | 기체 앞 **2.4 ~ 43.4 m** | 계산 | 화면 위 하향 34.7° ~ 아래 85.3° |
| 사람 크기 (30 m · 화면 중앙) | 서 있음 **51 px** · 누움 **56 px** | 계산 | 45~75° 전체 22~122 px |

### 성능 제약 (요구명세서)

| ID | 제약 | 설계 영향 |
|---|---|---|
| NFR-V01 | 추론 ≤30 ms / 프레임 | 타일 추론 + TensorRT FP16 필수 |
| NFR-V02 | ≥5 FPS | 프레임 대기열 제한 · 오래된 프레임 폐기 |
| NFR-V03 | 사람 AP50 ≥0.80 · **장소 분리** 평가 | 학습에 안 쓴 장소로만 판정 |
| NFR-V04 | 수평 오차 10~15 m | 자세 회전 포함 광선–지면 교차 |
| 통신 | 5 GHz 200 mW · 실효 약 2 km | 임무구역 크기 제한 |

### 미확정 항목 — 정해지면 바뀌는 곳

| 항목 | 상태 | 영향 받는 절 |
|---|---|---|
| 기본 마운트각 60° | `[잠정]` | 4.4 GeoResolver 오차 · 학습 데이터 구성 |
| 짐벌 피치 범위 · 카메라 장착 호환 | 실물 확인 대기 | 1.3 |
| 보조 렌즈 62°·78° 호환 | 실물 확인 대기 | 4.4 f 값 |
| 렌즈 실측 보정 | 렌즈 도착 후 | 4.4 f 값 |
| 4080 SUPER 추론 시간 | 미측정 | 5.1 NFR-V01 |
| 시연 구역·조도 | `[TBD]` | NFR-V03 달성 가능성 |

---

# 2. 파일 구조 또는 데이터베이스 설계

## 2.1 주요 엔티티

```
임무(Mission) 1──N 관측지점(Waypoint) 1──N 관측기록(Observation)
     │
     ├──N 프레임(Frame) 1──N 탐지(Detection) N──1 후보(Candidate) 1──N 스냅샷(Snapshot)
     ├──N 명령(Command) 1──N 명령응답(CommandAck)
     ├──N 알림(Alert)
     └──1 임무설정(MissionConfig) — 구역 · 고도 · 마운트각 · 렌즈 · 복귀조건 · 실행버전
```

## 2.2 핵심 테이블 `[잠정]`

| 테이블 | 주요 컬럼 | 설계 근거 |
|---|---|---|
| `mission` | id, 구역, 고도, 속도, 복귀조건, **실행버전**, 시작/종료시각, 종료사유 | UC-1201 — 편집본과 실행본 구분 |
| `mission_config` | mission_id, **렌즈(f·화각)**, **마운트각**, 탐지 임계값, 모델 버전 | 좌표 산정·재현에 필요 |
| `frame` | id, mission_id, 프레임식별자, 촬영시각(원본/추정), 기체 자세·위치, **짐벌각**, 추정방식·불확실성 | UC-0602 — 시각이 추정이면 남긴다 |
| `detection` | id, frame_id, bbox, 신뢰도, 클래스(person) | UC-0701 |
| `candidate` | id, mission_id, 대표좌표, **좌표유효/보류**, 상의색상 1·2순위, 운영자판정 | UC-0703·0706·0708 |
| `snapshot` | id, candidate_id, 파일경로, 촬영시각 | UC-0607 |
| `observation` | id, waypoint_id, 가시영역 폴리곤, 품질등급, 가림여부 | UC-0802 |
| `command` | id, mission_id, 종류, **제어세대**, 유효기간, 검증결과, 거부사유 | UC-0901·0902 |

- 좌표를 못 구한 후보도 **버리지 않고 `좌표보류`로 저장** (UC-1204)
- 짐벌각은 **프레임마다** 기록 — 비행 중 바뀌므로 임무설정 값만으로는 좌표를 못 푼다

## 2.3 파일 저장 정책

| 대상 | 위치 | 정책 | 근거 |
|---|---|---|---|
| 영상 녹화본 | 서버 | 임무 단위 · 용량·기간 순환 | UC-0605 |
| 게이트웨이 로컬 기록 | RPi microSD 64GB | 순환 · **서버 단절 중에도 계속** | UC-1203 |
| 스냅샷 | 서버 | 후보에 연결 · 영구 | UC-0607 |
| 탐지 모델 | 서버 | `.engine`(TensorRT) + 원본 `.pt` · 버전 기록 | 재현성 |

---

# 3. 사용자 인터페이스 설계

`[TBD]` — 관제 담당(이시원) 작성

| 화면 | 주요 요소 | 대응 UC |
|---|---|---|
| 로그인 | 인증, 권한 안내 | UC-0101~0103 |
| 관제 메인 | 지도 + 기체 상태 + 영상 + 후보 목록 | UC-0301·0201 |
| 임무 설정 | 구역 지정, 고도·속도·**마운트각**·복귀조건 | UC-0401~0405 |
| 후보 상세 | 스냅샷, 좌표(유효/보류), 상의 색상, **운영자 판정 입력** | UC-0703·0705 |
| 관측 완료도 | 미관측/유효/품질부족/가림/운용제한 **5색 레이어** | UC-0803 |
| 알림 | 목록·상세·확인 처리 | UC-1101~1104 |

---

# 4. 객체지향설계

## 4.1 Deployment Diagram (시스템 운용도)

```
┌──────────────────┐  5GHz 200mW    ┌──────────────────┐  인터넷   ┌──────────────────┐
│ 기체 F450        │ ◄────────────► │ 게이트웨이        │ ◄───────► │ 서버 4080 SUPER  │
│ H743 FC · M10    │ MAVLink·H.264  │ RPi 4B 2GB       │           │ 추론 · DB · API  │
│ IMX415 + 88° 렌즈│                │ 중계 · 순환 기록  │           │ vision · core    │
│ GM3 V2 짐벌      │                └──────────────────┘           └────────┬─────────┘
└──────────────────┘                                                        │ HTTPS·WebSocket
                                                                   ┌────────▼─────────┐
                                                                   │ 관제 웹 (브라우저) │
                                                                   └──────────────────┘
```

- 게이트웨이에서 **추론하지 않는다** — 2GB 메모리로는 TensorRT 를 못 올리고 NFR-V01 을 못 맞춘다
- 짐벌각은 FC 가 MAVLink 로 함께 보낸다 → 서버가 프레임과 시각을 맞춘다

## 4.2 Class Diagram (클래스 다이어그램)

### 패키지

```
control-web/   관제 웹          (이시원)
mission-core/  탐색·경로·안전   (서성훈)
vision/        영상 분석        (정서인)
gateway/       기체 중계        (공통)
common/        좌표·시각·메시지 (공통)
```

### vision 패키지 `[확정]`

```
FrameIngestor ──► TileSplitter ──► PersonDetector ──► DetectionMerger
      │                                                    │
      │ uses                                   ┌───────────┼───────────┐
      ▼                                        ▼           ▼           ▼
 common.TimeSync                         GeoResolver  ColorProfiler  SnapshotWriter
                                               │           │           │
                                               └────► CandidateRegistry ◄┘
                                                        │ uses
                                                        ▼
                                                 common.CameraModel
```

- 관제·비행 패키지 클래스는 담당자 작성 후 합친다 `[TBD]`

## 4.3 Sequence Diagram (시퀀스 다이어그램)

### 탐지 → 후보 등록 (UC-0701 → 0703) `[확정]`

```
기체        → 게이트웨이      : H.264 프레임 + MAVLink(자세·위치·짐벌각)
게이트웨이  → 서버            : 프레임 + 기체 상태 (시각 포함)
서버        → FrameIngestor   : accept(frame)
FrameIngestor                 : 시각 동기 · 오래된 프레임 폐기
FrameIngestor → TileSplitter  : split(frame)            → 타일 4장
TileSplitter  → PersonDetector: detect(tiles)            → Detection[]
PersonDetector → DetectionMerger : merge()               → 프레임 좌표 · NMS
DetectionMerger → GeoResolver : toWorld(bbox, 자세, 짐벌각, 고도) → LatLon | PENDING
DetectionMerger → ColorProfiler : profile(crop)          → 색상 1·2순위
DetectionMerger → SnapshotWriter : save()
CandidateRegistry : 같은 사람 병합 (거리·색상) → 신규/갱신
서버        → 관제 웹         : 후보 알림 + 스냅샷
```

- 기타 시퀀스 (임무 시작 · 제어권 인수 · 장애 복구) `[TBD]` — 관제·비행 담당

## 4.4 Design Classes (클래스 설계)

### vision 클래스 `[확정]`

| 클래스 | 책임 | 주요 속성 | 주요 연산 |
|---|---|---|---|
| `FrameIngestor` | 프레임 수신·시각 동기·대기열 제한 | 대기열 길이, 허용 지연 | `accept(frame)` · `dropStale()` |
| `TileSplitter` | 1920×1080 → 1280×720 타일 4장 (겹침 50 %) | 타일 크기, 겹침 | `split(frame) → Tile[]` |
| `PersonDetector` | TensorRT FP16 엔진 추론 | 엔진 경로, conf 0.15 | `detect(Tile[]) → Detection[]` |
| `DetectionMerger` | 타일 → 프레임 좌표 · NMS(IoU 0.6) | IoU 임계 | `merge(Detection[])` |
| `GeoResolver` | 발끝–지면 교차 좌표 | CameraModel, 지면 고도 | `toWorld(bbox, pose, gimbal, h) → LatLon \| PENDING` |
| `ColorProfiler` | 상의 Lab 12색 1·2순위 | 색상표 | `profile(crop) → Color[2]` |
| `SnapshotWriter` | 스냅샷 저장·후보 연결 | 저장 경로 | `save(frame, detection)` |
| `CandidateRegistry` | 후보 생성·병합·판정 상태 | 병합 거리 | `upsert(detection, latlon, color)` |
| `common.CameraModel` | 렌즈·짐벌 파라미터 | f, 화각, 마운트각 | `ray(u, v) → 3D 벡터` |

### 사람 탐지 (`PersonDetector` · `TileSplitter` · `DetectionMerger`) `[확정]`

| 항목 | 값 | 근거 |
|---|---|---|
| 모델 | YOLO11m · 1클래스(person) · COCO 사전학습 | 자세 분류는 도메인 이전 실패로 제외 (SARD 0.974 → Okutama 0.091) |
| 입력 | 1280×720 타일 4장 · 겹침 50 % | 단일 1920 입력 대비 AP50 **+2.9 %p** |
| 정밀도 | FP16 | FP32 대비 AP50 −0.04 %p · 시간 −35 % |
| 실행 | TensorRT 엔진 `imgsz=[736,1280]` | 정수 하나로 주면 정사각 엔진이 돼 이점이 사라진다 |
| 추론 시간 | **18.5 ms** (RTX 3090) | NFR-V01 여유 38 % |
| 임계값 | conf 0.15 · NMS IoU 0.6 | F2 최적 0.075~0.125 와 차이 ≤0.008 |

### 학습 설계 (60° 운용 기준) `[잠정]`

| 항목 | 값 |
|---|---|
| 학습 데이터 | 비스듬 시점 **≥50 %** (Unicamp-UAV · VisDrone 비스듬) + 수직 시점 (AI-Hub · WiSARD · NOMAD) · 장소당 상한 |
| 학습 크기 | 사람 31~94 px 로 맞춘 1280×720 크롭 (45~75° 운용 범위 역산) |
| 증강 | 상하 반전 없음 (60° 화면은 위=먼 쪽) · 회전 ±10° |
| 판정 평가셋 | **학습에 안 쓴 장소만** — test_obl (Okutama 비스듬 · 일본) · test_v2 (WiSARD 2곳 · 미국) · test_kr (AI-Hub · 한국) |
| 손실 개선 후보 | NWD 할당 · NWD+CIoU 회귀 · Wise-IoU v3 (작은 객체·가림) |

### 좌표 산정 (`GeoResolver`) `[확정]`

- 박스 **아래 변(발끝)** 을 지면 접촉점으로 본다 — 몸 중심은 지면 위라 더 멀게 나온다

```
f = (W/2) / tan(HFOV/2)                 88° 렌즈: f = 1,141 px
φ = θ + atan((v − H/2) / f)             θ = 마운트각 (90° = 수직 하향)
수평거리 d = h / tan φ
사선거리 R = h / sin φ
```

- 가로 방향은 3차원 광선을 **기체 자세(roll·pitch·yaw) + 짐벌각으로 회전**한 뒤 지면과 교차 — 각도를 더하기만 하면 기울었을 때 틀린다

**오차 예산** (고도 25 m · 화면 중앙 · GPS 3 m · 짐벌각 2° · 고도 3 m · 기복 5 m)

| 마운트각 | 합성 오차 | NFR-V04 (10~15 m) |
|---:|---:|---|
| 90° | 3.1 m | ✅ |
| **60° (기본)** | **4.7 m** | ✅ |
| 45° | 6.8 m | ✅ |
| 30° | 11.1 m | ⚠️ 경계 |

- 오차를 좌우하는 것은 짐벌각이 아니라 **지형 기복** (30° 에서 5 m 기복 → 8.7 m)
- 발끝이 가려지면 거리를 **짧게** 잡으므로 `좌표보류` 처리

### 관측 완료도 `[잠정]` — 비행 담당과 공동

- 렌즈 f · 마운트각 · 고도로 촬영 범위(60° · 30 m: 앞 2.4~43.4 m) 계산
- 품질 기준을 통과한 가시 영역만 누적
- 품질 부족·가림·운용 제한은 **분모에서 빼되 사유를 남긴다** (UC-0804)

---

# 5. 요구분석 참조표

## 5.1 비기능 요구 대응 (비전) 

| ID | 요구 | 설계 | 현재 | 판정 |
|---|---|---|---|---|
| NFR-V01 | 추론 ≤30 ms | 타일 4장 + TensorRT FP16 `[736,1280]` | **18.5 ms** (3090) | ✅ · 4080 SUPER 재측정 |
| NFR-V02 | ≥5 FPS | 대기열 제한 · 오래된 프레임 폐기 | 약 54 FPS (추론 기준) | ✅ |
| NFR-V03 | 사람 AP50 ≥0.80 · 장소 분리 | 장소 분리 학습 + test_v2 (1,407장 · 3,927명) | **0.2233** | ❌ 개선 중 |
| NFR-V04 | 수평 오차 10~15 m | 발끝–지면 교차 + 자세·짐벌 회전 | **4.7 m** (60° · 계산) | ✅ 실측 대기 |
| NFR-V05 | F2 기준 임계값 · 기본 0.15 | conf 0.15 | | ✅ |
| NFR-V06 | 오탐 ≤1건/프레임 | 하드 네거티브 학습 | 0.21~0.59 | ✅ |
| NFR-I01 | 관측 완료도 | 품질·가림 분모 제외 + 사유 기록 | | `[잠정]` |

- NFR-V03 원인: 같은 장소로 학습하면 **0.856**, 처음 보는 장소면 **0.223** → **장소 일반화가 병목**
- 대책: 장소 다양화(장소당 상한) · 60° 비스듬 데이터 비중 확대 · 작은 객체 손실 · 자체 촬영 파인튜닝

## 5.2 유스케이스 → 설계 요소

| UC | 설계 요소 | 설계 절 |
|---|---|---|
| UC-0601 영상 수신 | `gateway` | 4.1 |
| UC-0602 시각 동기 | `FrameIngestor` · `common.TimeSync` | 4.4 |
| UC-0603 프레임 선별 | `FrameIngestor.dropStale()` | 4.4 |
| UC-0605 녹화 | 서버 저장 정책 | 2.3 |
| UC-0607 스냅샷 | `SnapshotWriter` | 4.4 |
| UC-0701 사람 탐지 | `TileSplitter` · `PersonDetector` · `DetectionMerger` | 4.4 |
| UC-0702 좌표 산정 | `GeoResolver` · `common.CameraModel` | 4.4 |
| UC-0703 후보 등록 | `CandidateRegistry` | 4.3 · 4.4 |
| UC-0705 운영자 판정 | 관제 웹 · `candidate.운영자판정` | 2.2 · 3 |
| UC-0706 상의 색상 | `ColorProfiler` | 4.4 |
| UC-0801 촬영 범위 | `mission-core` · `common.CameraModel` | 4.4 |

- UC 84개 전체 표는 관제·비행 담당 보완 후 `[TBD]`

---

# 6. 참고 자료 및 용어 해설

## 6.1 참고 자료

- 요구명세서 v1.7 · 캡스톤 디자인 계획서 §3.2
- Ultralytics YOLO11 (v8.4.102) · NVIDIA TensorRT
- Wang et al., "A Normalized Gaussian Wasserstein Distance for Tiny Object Detection", arXiv 2110.13389 (2021)
- Wang et al., "Detecting tiny objects in aerial images: A normalized Wasserstein distance and a new benchmark", ISPRS J. (2022)
- Tong et al., "Wise-IoU: Bounding Box Regression Loss with Dynamic Focusing Mechanism", arXiv 2301.10051 (2023)
- 데이터셋: WiSARD · NOMAD · Okutama-Action · VisDrone · Unicamp-UAV · AI-Hub 182

## 6.2 용어

| 용어 | 뜻 |
|---|---|
| 마운트각 | 카메라 광축이 수평에서 아래로 기운 각. 90° = 수직 하향 |
| 대각/수평 화각 | 렌즈 표기(대각)를 16:9 화면 가로 기준으로 환산한 값. 88° → 80.2° |
| 타일 추론 | 1920×1080 을 1280×720 4장으로 잘라 각각 추론 후 NMS 병합 |
| AP50 | 박스 겹침 50 % 이상을 정답으로 볼 때의 평균 정밀도 |
| 장소 분리 | 학습에 쓴 장소를 평가에 쓰지 않는 분할 |
| 좌표보류 | 탐지는 됐으나 발끝이 안 보여 좌표를 못 구한 후보 |
| 제어세대 | 제어권이 바뀔 때마다 증가하는 번호. 오래된 명령 폐기 기준 |
| 실행버전 | 편집 중 설정과 실제 실행 중 설정을 구분하는 버전 |
